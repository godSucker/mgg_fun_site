name: Auto-Sync Mutants

on:
  schedule:
    - cron: '0 0 */3 * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-job:
    runs-on: ubuntu-latest

    steps:
      - name: 1. Клонирование репозитория
        uses: actions/checkout@v4

      - name: 2. Установка Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 3. Установка зависимостей
        run: npm ci

      - name: 4. Запуск Парсера (Режим FULL)

        run: npx tsx scripts/sync-mutants.ts full

      - name: 5. Сохранение изменений (Commit & Push)
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🤖 Auto-Sync: Найдены и добавлены новые мутанты"
          branch: main
          file_pattern: 'src/data/mutants/*.json public/textures_by_mutant/**'
