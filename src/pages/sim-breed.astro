---
import BaseLayout from '@/layouts/BaseLayout.astro';
import BreedingSim from '@/components/BreedingSim.svelte';
export const prerender = true;
---
<BaseLayout title="Скрещивания (m3 XML)">
  <section class="container mx-auto px-4 py-6">
    <p class="opacity-80 mb-4">Правила из <code>src/data/breeding/settings.xml</code>. Категория <code>three</code> трактуется как одно-гены (A, B, C).</p>
    <BreedingSim client:load />
  </section>
</BaseLayout>
