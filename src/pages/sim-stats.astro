---
import BaseLayout from '@/layouts/BaseLayout.astro';
import StatsCalculatorPro from '@/components/StatsCalculatorPro.svelte';
export const prerender = true;
---
<BaseLayout title="Статы мутантов (∞ уровни + сферы)">
  <section class="container mx-auto px-4 py-6">
    <p class="opacity-80 mb-4">Расчёт по формуле <code>scale(n) = n/10 + 0.9</code>. Уровень без лимита. Слоты сфер: герой — 4 обычных + 1 особая, остальные — 3 + 1.</p>
    <StatsCalculatorPro client:load />
  </section>
</BaseLayout>
